import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.chat.aula.pps',
  appName: 'Chat del Aula',
  webDir: 'www',
};

export default config;
